CREATE TABLE `mushroom_db` (
	`id` INT,
	`family` TEXT,
	`latin_names` TEXT,
	`english_names` TEXT,
	`notes` TEXT,
	`chemical_reactions` TEXT,
	`cap` TEXT,
	`nest` TEXT,
	`fruiting_body` TEXT,
	`upper_surface` TEXT,
	`flesh` TEXT,
	`outer_surface` TEXT,
	`habitat` TEXT,
	`gills` TEXT,
	`inner_layer` TEXT,
	`inner_surface` TEXT,
	`microscopic` TEXT,
	`underside` TEXT,
	`pores` TEXT,
	`stem` TEXT,
	`spore_mass` TEXT,
	`teeth` TEXT,
	`interior` TEXT,
	`eggs` TEXT,
	`name_origin` TEXT,
	`veil` TEXT,
	`odor` TEXT,
	`edibility` TEXT,
	`similar` TEXT,
	`taste` TEXT,
	`sources` TEXT,
	`spore_deposit` TEXT,
	PRIMARY KEY (`id`)
);